/* Public client configuration. The Supabase publishable/anon key is safe to expose. */
window.APP_CONFIG = window.APP_CONFIG || {
  supabaseUrl: '',
  supabaseAnonKey: '',
  appName: 'ReconX',
  autosave: true,
  requireSupabase: true
};
