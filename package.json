{
  "name": "permission-out",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "Production fiber route analysis and annual fee assessment workspace",
  "scripts": {
    "build": "node scripts/build.mjs",
    "build:local": "node scripts/build-local.mjs",
    "check": "node --check production.js && node --check src/worker.js && node scripts/validate.mjs && node scripts/test-worker.mjs",
    "preview": "node scripts/serve.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
